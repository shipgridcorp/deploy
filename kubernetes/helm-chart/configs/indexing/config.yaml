# indexing — EXAMPLE config override (not deployed by default).
# The service boots from its built-in defaults + the shared secrets/site env;
# a config file is only needed to override specific fields (config loader
# precedence: file → env → default). To enable, set `config: true` on
# services.indexing in values.yaml — the chart then mounts this file as a
# ConfigMap at CONFIG_PATH. Keep ONLY the keys you change.
#server:
#  http_port: 8000
#postgres:
#  dsn: postgres://shipgrid:shipgrid@postgres:5432/shipgrid_indexing?sslmode=disable
#connector:
#  grpc_addr: connector:9090
#ai:
#  gateway_url: http://gate:8000
#embedding:
#  concurrency: 4
#indexing:
#  poll_interval: 2s
